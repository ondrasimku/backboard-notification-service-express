version: '3.9'

services:
  notification-service:
    build: .
    container_name: notification-service-express
    ports:
      - '3002:3000'
    environment:
      - NODE_ENV=development
    volumes:
      - ./src:/usr/src/app/src
      - ./.env:/usr/src/app/.env:ro
      - ./jest.config.js:/usr/src/app/jest.config.js:ro
    networks:
      - backboard-network
networks:
  backboard-network:
    external: true
